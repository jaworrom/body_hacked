<%= form_for(@hack_tip) do |f| %>
  <% if @hack_tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hack_tip.errors.count, "error") %> prohibited this hack_tip from being saved:</h2>

      <ul>
      <% @hack_tip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
